import { formatDate, initials } from '../utils/formatters';
export default function ReviewCard({ review }) { return <div className="review-card"><div className="review-head"><span className="avatar avatar-sm">{initials(review.reviewer?.name)}</span><div><b>{review.reviewer?.name || 'Client'}</b><small>{formatDate(review.createdAt)}</small></div><span className="review-stars">{'★'.repeat(Math.round(review.rating || 5))}<span>{'★'.repeat(5 - Math.round(review.rating || 5))}</span></span></div><p>“{review.comment || 'A thoughtful, talented collaborator. Would absolutely work together again.'}”</p></div>; }
