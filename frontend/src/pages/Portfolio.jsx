import { ExternalLink, Plus, Sparkles } from 'lucide-react';
import { useState } from 'react';
import Button from '../components/Button';
const items = [{ title: 'Solace — a softer finance app', type: 'Product design', color: 'portfolio-lilac', tags: ['UX', 'Art direction'] }, { title: 'Oro house — identity system', type: 'Brand identity', color: 'portfolio-peach', tags: ['Branding', 'Strategy'] }, { title: 'Field notes — editorial platform', type: 'Web experience', color: 'portfolio-mint', tags: ['Web design', 'Motion'] }];
export default function Portfolio() { const [saved, setSaved] = useState(items); return <div className="dashboard-page"><div className="dashboard-title-row"><div><span className="eyebrow">Your body of work</span><h2>Make your work<br /><em>easy to remember.</em></h2></div><Button onClick={() => setSaved([...saved, { title: 'New case study', type: 'New project', color: 'portfolio-blue', tags: ['Add tags'] }])}><Plus size={17} /> Add project</Button></div><div className="portfolio-grid">{saved.map((item, index) => <article className="portfolio-item" key={`${item.title}-${index}`}><div className={`portfolio-art ${item.color}`}><Sparkles size={28} /><span>{String(index + 1).padStart(2, '0')}</span></div><div className="portfolio-item-copy"><span className="eyebrow">{item.type}</span><h3>{item.title}</h3><div className="skill-row">{item.tags.map((tag) => <span className="skill-badge" key={tag}>{tag}</span>)}<ExternalLink size={15} /></div></div></article>)}</div></div>; }
