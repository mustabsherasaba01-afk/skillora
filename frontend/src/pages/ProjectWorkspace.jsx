import { Activity, CalendarClock, CheckCircle2, File, MessageCircle, MoreHorizontal, Paperclip, Sparkles, Users } from 'lucide-react';
import { useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import Button from '../components/Button';
import { formatCurrency } from '../utils/formatters';
const tabs = [['overview', Activity], ['tasks', CheckCircle2], ['files', File], ['messages', MessageCircle]];
export default function ProjectWorkspace() { const { id } = useParams(); const [active, setActive] = useState('overview'); return <div className="workspace-project-page"><div className="workspace-project-head"><div><Link className="back-link" to={`/projects/${id}`}>← Back to project</Link><span className="eyebrow">Project workspace</span><h1>Solace — a softer<br /><em>finance app.</em></h1></div><Button variant="ghost"><MoreHorizontal size={17} /> More</Button></div><div className="workspace-project-tabs">{tabs.map(([label, Icon]) => <button className={active === label ? 'active' : ''} onClick={() => setActive(label)} key={label}><Icon size={15} /> {label}</button>)}</div><div className="workspace-project-grid"><main className="workspace-board"><div className="workspace-progress"><div><span className="eyebrow">Overall progress</span><strong>68%</strong></div><div className="progress-track"><span /></div><div className="progress-foot"><span>Started Jun 12</span><span>Due Aug 21</span></div></div><div className="workspace-section-heading"><div><span className="eyebrow">{active === 'overview' ? 'Current focus' : active}</span><h2>{active === 'overview' ? 'Make the complex feel calm.' : 'A clear space for the work.'}</h2></div><span className="status-badge status-in-progress">In progress</span></div><div className="workspace-task"><div className="task-check done"><CheckCircle2 size={16} /></div><div><b>Research synthesis</b><span>Key insights from 8 user conversations</span></div><span>Done</span></div><div className="workspace-task"><div className="task-check done"><CheckCircle2 size={16} /></div><div><b>Core visual direction</b><span>Palette, type, and initial moodboards</span></div><span>Done</span></div><div className="workspace-task"><div className="task-check"><span /></div><div><b>Prototype the first flow</b><span>Turn the direction into something tangible</span></div><span>In progress</span></div><div className="workspace-task"><div className="task-check"><span /></div><div><b>Prepare handoff</b><span>Assets, notes, and a little launch magic</span></div><span>Upcoming</span></div></main><aside><div className="workspace-info-card"><span className="eyebrow">Project details</span><div><CalendarClock size={16} /><span><b>Deadline</b>Aug 21, 2025</span></div><div><Users size={16} /><span><b>People</b>Maya + Amara</span></div><div><Sparkles size={16} /><span><b>Budget</b>{formatCurrency(1800)}</span></div><Link className="text-btn" to="/messages">Open messages <MessageCircle size={14} /></Link></div><div className="workspace-info-card"><span className="eyebrow">Shared files</span><div className="shared-file"><span className="file-icon"><File size={14} /></span><span><b>solace-brief.pdf</b><small>2.4 MB · Jun 12</small></span><Paperclip size={14} /></div><div className="shared-file"><span className="file-icon"><File size={14} /></span><span><b>visual-direction.fig</b><small>18.1 MB · Jun 18</small></span><Paperclip size={14} /></div></div></aside></div></div>; }
